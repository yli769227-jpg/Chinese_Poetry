# GRPO 韵律奖励微调分支依赖（unsloth_grpo_train.py + rewards/ + tests/）。
# 主线 nanoGPT 训练不需要这些；先装 requirements.txt 再按需装本文件。
# PyTorch 同样单独装，匹配 CUDA 版本。

# reward 计算（纯 Python，跨平台；--dry-run 与 pytest tests/ 只需要它）
pypinyin>=0.50

# 训练栈：GRPOTrainer 是 trl 0.14.0 才引入的，0.11~0.13 没有该类
trl>=0.14
transformers>=4.30
datasets>=2.14
# unsloth 仅在 CUDA + Linux 上有保证，macOS 不装
unsloth ; platform_system != "Darwin"
